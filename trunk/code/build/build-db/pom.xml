<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>	
	<groupId>com.dbdeploy</groupId>
	<artifactId>db-build</artifactId>
	<packaging>pom</packaging>
	<name>Exchange-Database</name>
	<version>1.0.2</version>
	<build>
		<pluginManagement>		    
			<plugins>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>properties-maven-plugin</artifactId>
					<version>1.0-alpha-2</version>
					<executions>
						<execution>
							<phase>initialize</phase>
							<goals>
								<goal>read-project-properties</goal>
							</goals>
							<configuration>
								<files>
									<file>db.properties</file>
								</files>
							</configuration>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>com.dbdeploy</groupId>
					<artifactId>maven-dbdeploy-plugin</artifactId>
					<version>3.0M3</version>
					<configuration>
						<driver>${driver}</driver>
						<dbms>${dbms}</dbms>
						<delimiter>/</delimiter>
						<delimiterType>row</delimiterType>
					</configuration>
					<dependencies>
						<dependency>
							<groupId>com.oracle</groupId>
							<artifactId>ojdbc14</artifactId>
							<version>10.2.0.3.0</version>
						</dependency>
						<dependency>
							<groupId>commons-lang</groupId>
							<artifactId>commons-lang</artifactId>
							<version>2.5</version>
						</dependency>
					</dependencies>                
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>properties-maven-plugin</artifactId>				
			</plugin>
		</plugins>	
	</build>
	<profiles>
		<profile>
			<id>phix</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>com.dbdeploy</groupId>
						<artifactId>maven-dbdeploy-plugin</artifactId>
						<executions>
							<execution>
								<id>phix_deploy</id>
								<phase>install</phase>
								<goals>
									<goal>db-scripts</goal>
									<goal>update</goal>
								</goals>
								<configuration>
									<url>${dburl_phix}</url>
									<userid>${userid_phix}</userid>
									<password>${password_phix}</password>
									<changeLogTableName>${changelog_phix}</changeLogTableName>
									<scriptdirectory>${patch_directory_phix}</scriptdirectory>
									<outputfile>${outputfile_phix}</outputfile>
									<undoOutputfile>${undoOutputfile_phix}</undoOutputfile>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>bpm</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>com.dbdeploy</groupId>
						<artifactId>maven-dbdeploy-plugin</artifactId>
						<executions>
							<execution>
								<id>bpm_deploy</id>
								<phase>install</phase>
								<goals>
									<goal>db-scripts</goal>
									<goal>update</goal>
								</goals>
								<configuration>
									<url>${dburl_bpm}</url>
									<userid>${userid_bpm}</userid>
									<password>${password_bpm}</password>
									<changeLogTableName>${changelog_bpm}</changeLogTableName>
									<scriptdirectory>${patch_directory_bpm}</scriptdirectory>
									<outputfile>${outputfile_bpm}</outputfile>
									<undoOutputfile>${undoOutputfile_bpm}</undoOutputfile>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
